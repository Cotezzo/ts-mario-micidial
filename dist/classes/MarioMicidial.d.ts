import { Client } from 'discord.js';
/**
 * Main class that rapresents the bot itself.
 * On init, logs in the bot into Discord and starts to listen on all the events.
 */
export declare class MarioMicidial extends Client {
    init: () => void;
}
